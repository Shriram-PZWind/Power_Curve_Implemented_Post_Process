

# --- Foundation loads (FNDLoads.sum) ---
# Reference elevation configurable via FND_REFERENCE (documentation only)
# For onshore: tower base sensors (TwrBs)
# For offshore: mudline sensors from SubDyn or equivalent
#
# Table 1A : Design extreme with PLF      — EXT/<sensor>.abs header
# Table 1B : Design characteristic no PLF — EXT/<sensor>.abs header
# Table 1C : Complementary loads with PLF — EXT/<sensor>_comp.abs header
# Table 2A : Lifetime RFC DEL             — FAT/<sensor>.rfc header
# Table 2B : Lifetime LDD DEL             — FAT/<sensor>.ldd header
[FOUNDATION]
#
# ── Reference (documentation only) ──────────────────────────────────────────
FND_REFERENCE        = Tower base (0.0m AGL)
#
# ── Foundation sensors ───────────────────────────────────────────────────────
FND_Mres             = mres_twrbs_[kn-m]
FND_Mx               = TwrBsMxt_[kN-m]
FND_My               = TwrBsMyt_[kN-m]
FND_Mz               = TwrBsMzt_[kN-m]
#FND_Fx               = TwrBsFxt_[kN]
#FND_Fy               = TwrBsFyt_[kN]
#FND_Fz               = TwrBsFzt_[kN]
#
# ── RFC DEL slopes for Table 2A (steel weld S-N) ─────────────────────────────
# Must be subset of RFC_M_VALUES in Table 0
FND_RFC_SLOPES       = 3.0  5.0
#
# ── LDD configuration for Table 2B ───────────────────────────────────────────
# Slopes must be subset of LDD_M_VALUES in Table 0
# Sensors must have LDD=1 in Table 2 — full sensor names, space separated
FND_LDD_SLOPES       = 3.0  5.0
FND_LDD_SENSORS      = TwrBsMxt_[kN-m]  TwrBsMyt_[kN-m]  TwrBsMzt_[kN-m]  TwrBsFxt_[kN]  TwrBsFyt_[kN]  TwrBsFzt_[kN]
