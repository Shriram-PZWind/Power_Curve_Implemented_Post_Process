# =============================================================================================================





# =============================================================================
# TABLE 4 — COMPONENT LOAD SUMMARY CONFIGURATION
# Parsed by the pipeline — defines sensor assignments for .sum output files
# References sensors from Table 2 (real) and Table 3A/3B (derived)
# Sections: [BLADE] [HUB] [PITCH_BEARING] [DRIVETRAIN] [YAW] [TOWER] [FOUNDATION]
# =============================================================================

# --- Blade loads (BldLoads.sum) ---
# Blade frame (b suffix) — rotates with rotor AND blade pitch
# Mx = edgewise, My = flapwise, Mz = torsion
# Auto-detected from OpenFAST output: RootMxb/Myb/Mzb + Spn1-9 MLxb/MLyb/MLzb
#
# Radial positions for spanwise gages (Spn1..Spn9):
#   The pipeline first tries [TOWER].TWR_ED_FILE (the ElastoDyn primary file
#   exposes TipRad / HubRad / BldNodes / NBlGages / BldGagNd, which gives
#   correct OpenFAST analysis-node positions). If that file is unavailable
#   it falls back to BLADE_FILE_PATH (a blade-properties file with BlFract);
#   that fallback is approximate. To get correct values, leave TWR_ED_FILE
#   set in [TOWER] and just keep BLADE_FILE_PATH below as a legacy hint.
[BLADE]
BLADE_FILE_PATH  = C:\Users\PZWind-AkashM\Desktop\setup\V7_Simulations\IEC_2A_\INPUTS\NRELOffshrBsline5MW_AeroDyn_blade.dat
BLADE_LENGTH     = 61.5
SUM_DEL_SLOPES   = 10.0  12.0  25.0
CONVENTION       = AUTO
# Radial fractions (%) for MainLoads.sum blade section — 4 stations
MAIN_BLD_FRACTIONS = 0  25  50  75
